# 2026-03-19 - 工作日志

## 日期
2026 年 3 月 19 日，星期四

## 今日工作内容

### 1. WorkflowExploder Operator 开发

**任务**：为 pipeline-yaml-generator 创建一个专门的 operator，用于将包含多个 workflow 的单行数据展开为多行。

**完成内容**：
- 创建 `dataflow-extensions/dataflow_extensions/operators/agentic/workflow_exploder.py`
- 实现核心功能：
  - 读取 `workflow.workflows` 数组
  - 对每个 workflow 生成独立的一行
  - 在 `warc_record_id` 后追加 workflow 索引（如 `id_0`, `id_1`），保证唯一性
  - 提取每个 workflow 的 `tools` 列表到独立字段
  - 在 workflow 字段中移除 tools，避免数据重复
- 所有配置参数在 `run()` 方法中指定，方便依赖分析
- 添加完整的文档和日志输出

**参数设计**：
- `input_workflows_key`: 输入 workflow 数组的字段名（默认 'workflow'）
- `output_workflow_key`: 输出 workflow 的字段名（默认 'current_task'）
- `output_tools_key`: 输出 tools 的字段名（默认 'candidate_tools'）

### 2. 更新 trajectory-generation.yaml

**任务**：将 WorkflowExploder 添加到 pipeline 配置中。

**完成内容**：
- 在 `operators` 部分添加 `op_workflow_exploder`
- 在 `pipeline.steps` 开头插入 `op_workflow_exploder`
- 配置使用默认参数

### 3. TrajectoryRuleValidator 测试开发

**任务**：为 TrajectoryRuleValidator 编写单元测试。

**完成内容**：
- 创建 `dataflow-extensions/test/operators/test_trajectory_validator.py`
- 测试覆盖：
  - 初始化测试（默认和自定义规则）
  - 工具定义验证（必需字段、名称格式、唯一性、schema 格式）
  - 工具调用验证（工具名存在、必需参数、未知参数、参数类型）
  - 对话格式验证（第一条消息必须是 system、turn order）
  - 完整流程测试
  - 使用真实 SmokeScreen 参考数据进行端到端测试
- 创建 MockStorage 类用于测试
- 添加 pytest 测试用例

## Token 使用统计

模型：Qwen3.5-122B-A10B

今日主要对话内容：
1. WorkflowExploder 开发讨论和实现
2. YAML 配置更新
3. 测试文件编写

详细 token 统计需要查看 API 日志。

## 备注

- 所有新创建的 operator 都遵循 dataflow 框架规范
- 参数在 run 方法中指定，便于框架依赖分析
- 测试文件使用 pytest 框架，方便后续 CI/CD 集成

---

## 下午工作 (15:23 - 16:52)

### 1. arXiv:2601.10355 论文研究

**任务**：阅读论文，查找 TrajectoryRuleValidator 中 valid_transitions 和 invalid_transitions 的参考值。

**发现**：
- 论文 Section 3.3 定义了对话转换规则
- 有效转换 (5 种): system→user, user→assistant, assistant→tool, tool→assistant, assistant→user
- 无效转换 (11 种): tool→user (论文明确禁止), assistant→assistant, user→user, 以及其他 system 相关组合
- 论文第 482-490 行提到："Tool response cannot followed by user message"

### 2. 更新 trajectory-generation.yaml 配置

**文件 1**: `pipeline-yaml-generator/references/trajectory-generation.yaml`
- 添加论文引用注释
- 为每个 transition 规则添加详细注释

**文件 2**: `dataflow-extensions/configs/trajectory-generation.yaml`
- 添加缺失的 `["assistant", "user"]` 转换
- 添加所有 11 种 invalid_transitions（包括之前未提及的 5 种）
- 添加详细的规则注释

**提交**: 已推送到 dataflow-extensions 仓库

### 3. 分析 00000001.jsonl 验证失败原因

**发现**: 63 条记录中有 3 条失败，共 49 个无效转换
- `tool → user`: 35 次 (最主要问题)
- `tool → tool`: 10 次
- `assistant → assistant`: 4 次

**根本原因**: Trajectory Refiner (Stage 4) 生成时违反了转换规则

### 4. 强化 Trajectory Refiner prompt

**修改**: `dataflow-extensions/configs/trajectory-generation.yaml` 中的 Stage 4 prompt
- 添加 **CRITICAL: Turn Order Rules** 部分
- 包含 VALID/INVALID Transitions 表格
- 列出 COMMON MISTAKES TO AVOID
- 提供正误示例对比

**提交**: 已推送到 dataflow-extensions 仓库

### 5. PartitionPipelineParallelRun 依赖简化功能开发

**文件**: `dataflow/dataflow/pipeline/Pipeline.py`

**新增方法**:
1. `_is_dependent_on(wl1, wl2, dependencies, visited)`: 递归检查 workload 之间的依赖关系
2. `_simplify_dependencies(dependencies)`: 消除传递依赖

**简化逻辑**:
- 如果 A 依赖 {B, C} 且 B 依赖 C
- 简化后：A 只依赖 B（因为执行 B 时会自动确保 C 先完成）

**修改位置**: `concurrent_execute_operators()` 方法
- 构建依赖图后调用 `_simplify_dependencies()`
- 使用简化后的依赖图进行并发执行
- 记录简化前后的依赖数量对比
- 打印 partition=1 的依赖图用于调试

**提交**: 已 rebase 并推送到 dataflow 仓库

---

## 17:00 - 17:20 工作记录

### 1. NestExtractOperator 参数位置调整

**任务**: 将 `**kwargs` 从 `__init__` 移动到 `run` 方法，方便进行依赖分析。

**修改文件**: `dataflow-extensions/dataflow_extensions/operators/core/nested_extract.py`

**完成内容**:
- `__init__` 方法简化，不再解析配置参数
- `run` 方法签名改为 `run(self, storage, remove_all_keys=False, **kwargs)`
- 配置参数解析逻辑移到 `run` 方法开头
- 使用局部变量 `keys_map` 替代 `self.keys_map`

**原因**: 框架需要在运行时分析输入输出依赖关系，参数在 `run` 方法中更便于分析。

### 2. 添加字段过滤功能

**任务**: 在 pipeline 中添加字段过滤，只保留 9 个核心字段。

**修改文件**:
- `dataflow-extensions/configs/trajectory-generation.yaml`
- `pipeline-yaml-generator/references/trajectory-generation.yaml`

**新增算子**: `op_field_extractor` (使用 NestExtractOperator)

**配置**:
```yaml
params:
  remove_all_keys: true  # 只保留提取的字段
keys:
  input:
    input_warc_record_id_key: "warc_record_id"
    input_candidate_tools_key: "candidate_tools"
    ...
  output:
    output_warc_record_id_key: "warc_record_id"
    output_candidate_tools_key: "candidate_tools"
    ...
```

**保留的 9 个字段**:
- `warc_record_id`
- `candidate_tools`
- `current_task`
- `text`
- `language`
- `url`
- `multistep_analysis`
- `refined_trajectory`
- `hallucination_score`

### 3. TrajectoryRuleValidator 鲁棒性修复

**问题**: `TypeError: unhashable type: 'list'` - 某些 JSON schema 中 `type` 字段是数组形式（如 `["string", "null"]` 表示可选字段）。

**修复**: `dataflow-extensions/dataflow_extensions/operators/agentic/trajectory_validator.py`

**修改内容**:
```python
# 处理 type 可能是数组的情况
if isinstance(param_type, list):
    # 数组形式：检查所有类型是否都有效
    for t in param_type:
        if t not in self.valid_param_types:
            # 记录错误
            ...
elif param_type not in self.valid_param_types:
    # 字符串形式的类型检查
    ...
```

**提交**:
- ✅ Submodule `dataflow-extensions` 已 push (`5704e75`)
- ✅ 主仓库已 commit (`9c57b8d`)

---

## 总结

今日主要完成 trajectory-generation pipeline 的完整开发和优化：

1. **WorkflowExploder** - 展开多 workflow 数据
2. **NestExtractOperator 优化** - 参数位置调整
3. **字段过滤** - 只保留核心字段
4. **TrajectoryRuleValidator 修复** - 处理数组类型 type 字段
5. **Turn Order 规则强化** - 更新 prompt 和验证规则

所有修改已提交并推送到相应仓库。
