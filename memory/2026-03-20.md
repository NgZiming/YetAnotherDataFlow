# 2026-03-20 - pipeline-yaml-generator skill 全面重构

## 主要工作

**1. 仓库自动更新功能**
- 准备阶段自动检查并更新 `dataflow` 和 `dataflow-extensions` 仓库到 main 分支
- 使用 `git fetch origin main` + `git reset --hard origin/main` 确保使用最新 operators

**2. Operators 文档完善（123+ 个）**
- 为所有 operators 添加完整的中文描述
- 包含初始化参数、运行参数、YAML 配置示例
- 覆盖 14 个分类文件

**3. FormatStrPromptedGenerator 优先使用**
- 明确提示其他 agent 优先使用 `FormatStrPromptedGenerator` 而非 `PromptedGenerator`
- 原因：支持多字段映射，清晰表达 operator 间的依赖关系

**4. Filter 隐藏式依赖说明**
- 说明 `PromptedFilter` 等 Filter 会直接丢弃不满足条件的数据
- 提供数据保留率估算表

**5. 推理超参数默认值**
- temperature: 0.7（生成 0.7，提取/分类 0.1）
- max_completion_tokens: 8192
- max_workers: 100

**6. 并发执行合并步骤提醒**
- `PartitionPipelineParallelRun` 并发执行后需要 `NestExtractOperator` 合并结果

**7. 流程简化（7 步 → 5 步）**

## 文件位置
- `dataflow-skills/pipeline-yaml-generator/SKILL.md` - 主仓库
- 本地 OpenClaw skill 目录已同步

## 关键改进点
1. 仓库自动更新到 main 分支
2. 所有 operators 都有详细描述和示例
3. FormatStrPromptedGenerator 优先，依赖关系更清晰
4. Filter 会丢弃数据，需要估算保留率
5. 并发执行需要 NestExtractOperator 合并
